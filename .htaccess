RewriteEngine On
RewriteCond %{HTTPS} !=on
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301,NE]

Options +FollowSymlinks
RewriteEngine on
RewriteRule ^profile/([^/]+) /php/profile.php?user=$1 [R,NC]